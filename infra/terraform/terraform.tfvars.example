# Copy to terraform.tfvars and fill in. terraform.tfvars is git-ignored.

key_name = "ammunation-deploy"   # an EC2 key pair that already exists in `region`
ssh_cidr = "203.0.113.4/32"      # your public IP, /32

# region        = "ap-south-1"
# instance_type = "t3.small"
